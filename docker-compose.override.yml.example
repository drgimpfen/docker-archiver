services:
  app:
    # This override file is intended for local development mounts and overrides.
    volumes:
      # Development stack directories (adjust to your setup)
      # Required: use identical host:container paths (e.g., /opt/stacks:/opt/stacks)
      - /opt/stacks:/opt/stacks
      # Add more as needed:
      # - /srv/docker/stacks:/srv/docker/stacks
      # - /home/user/docker:/home/user/docker
